/** Renderer-local sprite identifiers for the supplied structure atlases. */
export enum EdgeStructureSprite {
  StoneWall = "stone-wall",
  TimberWall = "timber-wall",
  ClosedDoor = "closed-door",
  OpenDoor = "open-door",
  Window = "window",
}

/** Props atlas identifiers. Tree variation can be added without map-schema changes. */
export enum PropsSprite {
  OakTree = "oak-tree",
}
